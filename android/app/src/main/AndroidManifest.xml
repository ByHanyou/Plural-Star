<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" android:maxSdkVersion="29" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" android:maxSdkVersion="32" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />
    <!-- NO exact-alarm permissions on purpose. Play restricts USE_EXACT_ALARM to
         apps whose CORE function is an alarm clock, timer or calendar; a
         front-check reminder is a secondary feature, so declaring it risks
         rejection. SCHEDULE_EXACT_ALARM is user-grantable but unnecessary here:
         reminders use AlarmType.SET_AND_ALLOW_WHILE_IDLE, which survives Doze
         without needing either permission.
         The remove below strips the copy the notification library's own manifest
         merges in (both notifee 9.1.8's AAR and the notify-kit fork declare it) —
         without this, the permission ships in the APK despite our choice. -->
    <uses-permission android:name="android.permission.SCHEDULE_EXACT_ALARM" tools:node="remove" />
    <!-- Reminders survive a reboot. Verified in the packaged notifee core AAR:
         it declares RECEIVE_BOOT_COMPLETED and a receiver for BOOT_COMPLETED /
         QUICKBOOT_POWERON itself, so no receiver of ours is needed — manifest
         merging brings both in. Declaring the permission here too is harmless
         and makes the dependency explicit. -->
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />

    <application
      android:name=".MainApplication"
      android:label="@string/app_name"
      android:icon="@mipmap/ic_launcher"
      android:roundIcon="@mipmap/ic_launcher_round"
      android:allowBackup="false"
      android:theme="@style/AppTheme"
      android:usesCleartextTraffic="true"
      android:requestLegacyExternalStorage="true">

      <activity
        android:name=".MainActivity"
        android:label="@string/app_name"
        android:configChanges="keyboard|keyboardHidden|orientation|screenLayout|screenSize|smallestScreenSize|uiMode"
        android:launchMode="singleTask"
        android:windowSoftInputMode="adjustPan"
        android:exported="true">
        <intent-filter>
            <action android:name="android.intent.action.MAIN" />
            <category android:name="android.intent.category.LAUNCHER" />
        </intent-filter>
      </activity>

      <meta-data
          android:name="com.google.firebase.messaging.default_notification_icon"
          android:resource="@drawable/ic_stat_notification" />
      <meta-data
          android:name="com.google.firebase.messaging.default_notification_color"
          android:resource="@android:color/holo_orange_dark" />

      <service
          android:name="app.notifee.core.ForegroundService"
          android:foregroundServiceType="specialUse"
          android:exported="false">
          <property
              android:name="android.app.PROPERTY_SPECIAL_USE_FGS_SUBTYPE"
              android:value="Persistent front-status notification and end-to-end encrypted friend/device sync connection" />
      </service>

    </application>
</manifest>